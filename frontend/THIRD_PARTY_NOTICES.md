# Third-Party Notices

This frontend uses open-source React UI primitives and utility libraries.

## shadcn/ui and Radix UI

Several reusable UI primitives in `src/app/components/ui` are adapted from the
shadcn/ui ecosystem, which is distributed under the MIT license and built on
Radix UI primitives.

## Other Dependencies

Runtime and development dependencies are listed in `package.json`. Their exact
resolved versions are recorded in `package-lock.json`.
